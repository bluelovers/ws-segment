{
  "name": "@novel-segment/postag",
  "version": "1.0.2",
  "description": "",
  "keywords": [],
  "homepage": "https://github.com/bluelovers/ws-segment/tree/master/packages/@novel-segment/postag#readme",
  "bugs": {
    "url": "https://github.com/bluelovers/ws-segment/issues"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/bluelovers/ws-segment.git"
  },
  "license": "ISC",
  "author": "bluelovers",
  "main": "index.js",
  "directories": {
    "lib": "lib"
  },
  "scripts": {
    "lint": "ynpx --quiet eslint -- **/*.ts",
    "test": "jest",
    "test:jest": "ynpx --quiet jest -- --coverage",
    "test:mocha": "ynpx --quiet -p ts-node -p mocha mocha -- --require ts-node/register \"!(node_modules)/**/*.{test,spec}.{ts,tsx}\"",
    "preversion": "yarn run prepublishOnly",
    "prepublishOnly": "yarn run prepublishOnly:check-bin && yarn run test",
    "prepublishOnly:check-bin": "ynpx --quiet @yarn-tool/check-pkg-bin",
    "prepublishOnly:update": "yarn run ncu && yarn run sort-package-json",
    "ncu": "yarn-tool ncu -u",
    "sort-package-json": "yarn-tool sort"
  },
  "dependencies": {
    "ts-enum-util": "^4.0.2",
    "ts-type": "^1.2.32"
  },
  "publishConfig": {
    "access": "public"
  }
}
