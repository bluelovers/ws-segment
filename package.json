{
  "name": "@novel-segment/api-server",
  "version": "1.0.35",
  "private": true,
  "description": "simple segment api server",
  "homepage": "https://github.com/bluelovers/ws-segment#readme",
  "bugs": {
    "url": "https://github.com/bluelovers/ws-segment/issues"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/bluelovers/ws-segment.git"
  },
  "license": "ISC",
  "author": "",
  "main": "index.js",
  "scripts": {
    "coverage": "npx nyc npm run test",
    "lint": "npx eslint **/*.ts",
    "test": "echo \"Error: no test specified\" && exit 1",
    "npm:publish": "npm publish",
    "postpublish_": "git commit -m \"publish new version\" .",
    "postpublish:commit": "ts-node ./script/publish-after2.ts",
    "postpublish:done": "yarn run now-deploy",
    "ncu": "npx yarn-tool ncu -u",
    "now-build": "yarn run now-build:install && yarn run now-build:run",
    "now-build:install": "yarn add novel-segment vercel",
    "now-build:run": "node ./build.js",
    "now-deploy": "yarn run now-build:run && npx vercel deploy",
    "sort-package-json": "npx yarn-tool sort",
    "tsc:default": "tsc -p tsconfig.json",
    "tsc:esm": "tsc -p tsconfig.esm.json"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "crlf-normalize": "^1.0.5",
    "express": "^4.17.1",
    "fs-extra": "^10.0.0",
    "tslib": "^2.3.0"
  },
  "devDependencies": {
    "@types/cors": "^2.8.10",
    "@types/express": "^4.17.12",
    "@types/fs-extra": "*"
  },
  "peerDependencies": {
    "novel-segment": "^2",
    "vercel": "^23.0.1"
  },
  "engines": {
    "node": ">=10"
  }
}
